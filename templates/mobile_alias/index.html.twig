{% extends "admin/layout.html.twig" %}

{% block content %}
<h2>Mobile Alias: {{ entities|length }}</h2>

{% if error != null %}
    <div class="alert alert-danger" role="alert">
        <i class="fa fa-exclamation-circle"></i>
        {{ error }}
    </div>
{% endif %}

<div class="buttonbox">
    <a href="{{ path('mobile_alias_add') }}" class="btn btn-primary" role="button">
        <i class="fa fa-plus"></i>
        Create Mobile Alias
    </a>
</div>

    <table class="table">
        <tr>
            <th style=" text-align: center">#id</th>
            <th style=" text-align: center">Mobile</th>
            <th style=" text-align: center">Mobile Alias</th>
            <th style=" text-align: center">Description</th>
            <th style=" text-align: center">Action</th>
        </tr>
        {% for entity in entities %}
        <tr>
            <td style="text-align: center" >{{ entity.id }}</td>
            <td style="text-align: center" >{{ entity.mobile }}</td>
            <td style="text-align: center" >{{ entity.mobile_alias }}</td>
            <td style="text-align: center" >{{ entity.description|raw|nl2br }}</td>
            <td>
                <a href="{{path('mobile_alias_edit', { 'id': entity.id })}}"  title="Edit" ><i class="glyphicon glyphicon-edit"></i></a>
                <a href="{{path('mobile_alias_delete', { 'id': entity.id })}}" onclick="return confirm('Are you sure?');" title="Delete" ><i class="fa fa-remove"></i></a>
            </td>
        </tr>
        {% endfor %}
    </table>
{% endblock %}
